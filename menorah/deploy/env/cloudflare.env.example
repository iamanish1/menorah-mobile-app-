# Copy this file to cloudflare.env on the Ubuntu host. Never commit cloudflare.env
# or the token file. The file must contain only the rotated tunnel token.

CLOUDFLARE_TUNNEL_TOKEN_FILE=/opt/menorah/secrets/cloudflare-tunnel-token
